-module(problem<%= number %>_tests).

-include_lib("eunit/include/eunit.hrl").
-import(problem<%= number %>, [<%= function %>/1]).

<%= function %>_test_() -> [
% assertions.each do |expected, computation|
  ?_assertEqual(<%= expected %>, <%= function %>(<%= computation %>)),
% end
  ?_assert(false) % FIXME: ensures you start with a redbar!
  ].
