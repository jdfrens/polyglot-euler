-module(problem<%= number %>_main).

-export([main/1]).
-import(problem<%= number %>, [<%= problem_function %>/1]).

main([A]) -> 
	I = list_to_integer(atom_to_list(A)), 
	S = <%= problem_function %>(I), 
	io:format("~w~n", [S]), 
	init:stop().
