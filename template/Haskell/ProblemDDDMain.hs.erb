module Main where

import Data.String
import System.Environment
import System.Exit

import Problem<%= number %>

main = getArgs >>= putAnswers . (map <%= function %>) . parse
  where
    parse args     = map read args
    putAnswers l   = putStr $ unlines $ map show l
