require "test/unit"

require "problem<%= number %>"

class TestProblem<%= number %> < Test::Unit::TestCase

% functions.each do |function|
  def test_<%= function %>
% tests(function).each do |expected, computation|
    assert_equal <%= expected %>, <%= computation %>.<%= function %>
% end
  end
  
% end
end