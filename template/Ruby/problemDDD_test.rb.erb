require "test/unit"

require "problem<%= number %>"

class TestProblemDDD < Test::Unit::TestCase

% functions.each do |function|
  def test_<%= function %>
% tests(function).each do |expected, computation|
    assert_equal <%= expected %>, <%= computation %>.<%= problem_function %>
% end
  end
  
% end
end